import { defineConfig } from "unocss";

export default defineConfig({
  theme: {
    colors: {
      sb: {
        blue: "#5AB0FF",
        yellow: "#FAED00",
        green: "#34E234",
        darkGreen: "#12C512",
      },
    },
  },
});
